# Copy these templates — never commit filled secrets.
#
#   backend/.env.example  →  backend/.env
#   frontend/.env.example →  frontend/.env.local
#
# See README.md for provider setup (Neon, Google OAuth, OpenAI).
